class Solution {
    public int maximumProduct(int[] nums) {
        Arrays.sort(nums);
        int na=nums[0]*nums[1]*nums[nums.length-1];
        int vi=nums[nums.length-3]*nums[nums.length-2]*nums[nums.length-1];
        if(na>vi){
            return na;
        }
        return vi;
    }
}
