class Solution {
    public int removeDuplicates(int[] arr) {
      int lens=arr.length;
      int i=0;
     if(lens==0){
        return 0;
     } 
     for(int j=1;j<arr.length;j++){
        if(arr[j]!=arr[i]){
            arr[++i]=arr[j];
        }
     }
     return i+1;
    }
}
