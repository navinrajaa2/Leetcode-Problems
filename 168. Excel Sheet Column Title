class Solution {
    public String convertToTitle(int columnNumber) {
        StringBuilder navin = new StringBuilder();
        while (columnNumber > 0) {
            columnNumber--;
            char ch = (char) ('A' + columnNumber % 26);
            navin.append(ch);
            columnNumber /= 26;
        }
        return navin.reverse().toString();
    }
}
