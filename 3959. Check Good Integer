class Solution {
    public boolean checkGoodInteger(int n) {
        int sum=0;
        int squsum=0;
        while(n>0){
            int digit=n%10;
            sum+=digit;
            squsum+=digit*digit;
            n/=10;
        }
        return (squsum-sum) >=50;
    }
}
