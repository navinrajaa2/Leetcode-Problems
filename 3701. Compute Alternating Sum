class Solution {
    public int alternatingSum(int[] nums) {
        int answer=0;
        int sign=1;
        for(int ele:nums){
            answer+=ele*sign;
            sign*=-1;
        }
        return answer;
    }
}
